<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>de.team33.testing</groupId>
        <artifactId>testing-root</artifactId>
        <version>1.1.0</version>
    </parent>

    <artifactId>testing-home</artifactId>
    <packaging>jar</packaging>

    <name>Team33 Testing Library Home</name>
    <description>team33 testing library collection - documents home</description>

    <properties>
        <skipHome>true</skipHome>
        <skipDeploy>true</skipDeploy>
    </properties>

    <build>
        <resources>
            <resource>
                <directory>${basedir}/src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
            <resource>
                <directory>${basedir}/../target/home-collected</directory>
                <filtering>false</filtering>
            </resource>
        </resources>
    </build>
</project>
